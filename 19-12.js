//19-12 Fnction Event, Event bubble module summary

/*
    19-1 When to use a function, use function inside a loop (take an array and double it, make a multiplication table (নামতা), Double only even number,  )

*/