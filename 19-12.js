//19-12 Fnction Event, Event bubble module summary

/*
    19-1 When to use a function, use function inside a loop (take an array and double it, make a multiplication table (নামতা), Double only even number, //If else একটা function এ নিয়ে forloop এর মধ্যেও function call করা যায়।  )

    19-2 Again When to return from a function and from where (When use return and when not, make a function use if else and return, make a result squire )

    19-3 Callback function and pass different function ()

*/