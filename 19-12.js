//19-12 Fnction Event, Event bubble module summary

/*
    19-1 When to use a function, use function inside a loop (take an array and double it, separate even and odd number from an array, make a multiplication table (নামতা), Double only even number by Function Invocation and function, //If else একটা function এ নিয়ে forloop এর মধ্যেও function call করা যায়।  )

    19-2 Again When to return from a function and from where (When use return and when not, make a function use if else and return, make a result squire )

    19-3 Callback function and pass different function (Work same but nam and age different, Synchronous Callbacks)

    19-4 arguments and deal with unknown number of arguments (add morethan 2 numbers)

    19-5 How to organize code inside a function (function writing style)

    19-6 What is Event, different types of event in web (Mouse click event, keyboard key press event, browser event, Mouse move event.)

    19-7 add event handler directly on an element (onclick, onmousemove, onmouseout) (Add click event on a button using function)

    19-8 add event listener using javascript (click a button to change background color)

    19-9 Event bubble with example (https://www.salesforcetroop.com/lwc/event-with-bubbling-child-to-parent-component)

    19-10 Stop propagating event bubble (stop the bubble system: event.stopPropagation(এটা উপরে যাওয়া অফ করে দিবে); event.stopImmediatePropagation(আশে পাশের গুলাও বন্ধ করে দিবে);)

    19-11 Event delegate and purpose of Event bubble (Add new list item and remove by click)

*/